@model PassionProject.Models.Problem

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Problem</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ProblemName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ProblemName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.ProblemGrade)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ProblemGrade)
        </dd>
        @Html.ActionLink("Back to List", "List")
    </dl>

    <form method="POST" action="@Url.Action("Delete", "Problem", new { id = Model.ProblemID })">
        @Html.AntiForgeryToken()
        <div class="form-actions no-color">
            <button type="submit" class="delbutton">
                Confirm Delete
            </button>
        </div>
    </form>
</div>
